{
  "name": "server",
  "version": "1.0.0",
  "description": "the is a realtime codeshare",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "Gautam Singh Jadon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "socket.io-client": "^4.7.2"
  }
 "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/" // Replace "tsc" with your actual build command
  }
}
